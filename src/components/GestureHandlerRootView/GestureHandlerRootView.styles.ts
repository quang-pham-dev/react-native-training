import styled from 'styled-components/native'
import {GestureHandlerRootView} from 'react-native-gesture-handler'

export const GestureHandlerRootViewStyled = styled(GestureHandlerRootView)`
  flex: 1;
`
